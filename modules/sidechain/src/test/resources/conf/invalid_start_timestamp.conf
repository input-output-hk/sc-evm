include required("test-chain.conf")

sidechain {
  cardano-follower {
    epoch-duration-seconds = 432000
  }
}

# Genesis JSON definition
genesis-data = { include required("invalid_start_timestamp.json") }
