# Time between stable blocks metric

## Definition

The amount of time it takes for a node to register two consecutive stable blocks.

## Metric

Formula: `sc_evm_consensus_time_between_stable_blocks{namespace="$namespace"})`
